# JMapBrowser
A simple swing client to inspect jmap servers. \
It supports viewing all supported capabilities and their configuration.
It also supports viewing the [filenode](https://datatracker.ietf.org/doc/draft-ietf-jmap-filenode/) capability as a tree view. \
Currently there is no support for changing or calling stuff